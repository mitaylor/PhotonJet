std::string purity = abcd_pp.root #tessellate_pp.root
std::string type = pp_pthf

std::vector<std::string> inputs = \
    photon_isolation_study_pp_raw.root \
    photon_isolation_study_pp_bkg.root

std::vector<std::string> groups = raw bkg
std::vector<std::string> labels = \
    pjet_f_dr

std::string tag = pure
